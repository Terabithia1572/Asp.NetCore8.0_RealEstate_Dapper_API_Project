@model List<PropertyImageDTO>

<div class="single-post-image mb-5">
    <div class="owl-blog owl-carousel owl-theme">
        @foreach (var item in Model)
        {
            <div class="item">
                <div class="card">
                    <img src="@item.ProductImageURL" style="height:500px" class="img-fluid radius-image" alt="image">
                </div>
            </div>
        }
       

    </div>
</div>